# Frontend Launch Console

Static demo UI for launch parameter configuration, stack deployment placeholders, baseline-vs-JIT simulation, and phase monitoring.

Open with any static server, for example:

```bash
python3 -m http.server 4173 --directory frontend
```

Then browse `http://localhost:4173`.
